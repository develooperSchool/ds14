// const utils=require("../../utils/app.utils")

// const getRevenuCategoryByIdValidation = (req, res, next) => {
//     if (utils.isInvalidId(req.params.id)) res.status(400).send("INVALID ID");
//     else next();
//   };

//   module.exports ={
//     getRevenuCategoryByIdValidation
//   }
